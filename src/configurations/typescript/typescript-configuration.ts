import type { OverridesProperty } from '../../types/overrides-property';
import type { TypeScriptConfiguration } from '../../types/typescript-configuration';

export type TypescriptConfiguration = OverridesProperty & TypeScriptConfiguration;
