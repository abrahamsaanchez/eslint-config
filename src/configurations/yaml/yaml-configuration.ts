import type { OverridesProperty } from '../../types/overrides-property';
import type { StylisticProperty } from '../../types/stylistic-property';

export type YamlConfiguration = OverridesProperty & StylisticProperty;
